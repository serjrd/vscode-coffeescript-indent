{
	"name": "coffeescript-indent",
	"displayName": "coffeescript-indent",
	"description": "Auto indent on press enter after keywords like if, else if, else, switch, when, default, ()-> ..",
	"version": "0.0.1",
	"publisher": "serjrd",
	"repository": {
		"type": "git",
		"url": "https://github.com/serjrd/coffeescript-indent"
	},
	"engines": {
		"vscode": "^1.21.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onLanguage:coffeescript"
	],
	"main": "./extension",
	"scripts": {
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "node ./node_modules/vscode/bin/test"
	}
}